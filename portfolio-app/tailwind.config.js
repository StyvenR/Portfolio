/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // Essentiel pour le fonctionnement du mode sombre
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      // Vos extensions de thème
    },
  },
  plugins: [],
}